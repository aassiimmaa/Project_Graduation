import { ALIGN_CENTER } from "../constant"

const styleDialogTitle = {
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: ALIGN_CENTER
}

const styleDialogContent = { textAlign: ALIGN_CENTER }

export { styleDialogTitle, styleDialogContent }
