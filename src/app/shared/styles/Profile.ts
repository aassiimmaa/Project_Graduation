import { ALIGN_CENTER } from "../constant"

const styleContainerProfile = { mt: 20 }

const stylePaperProfile = { p: 4, textAlign: ALIGN_CENTER, m: 4 }

const styleAvatarProfile = {
  width: 150,
  height: 150,
  margin: '0 auto',
  mb: 2
}

const styleUploadAvatarButton = {
  position: 'absolute',
  bottom: 0,
  right: 0,
  backgroundColor: 'white',
  boxShadow: 2
}

const styleForm = { mt: 3 }

const styleActions = {
  mt: 3,
  display: 'flex',
  justifyContent: 'space-between'
}

export {
    styleContainerProfile,
    stylePaperProfile,
    styleAvatarProfile,
    styleUploadAvatarButton,
    styleForm,
    styleActions
};