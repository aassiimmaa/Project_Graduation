import { ALIGN_CENTER, FONT_WEIGHT_BOLD, TEXT_COLOR } from '../constant'

const styleLayoutService = {
  paddingY: '60px'
}

const styleTitle = {
  color: TEXT_COLOR,
  fontWeight: FONT_WEIGHT_BOLD,
  textAlign: ALIGN_CENTER,
  marginBottom: '48px'
}

const styleServiceSectionLayout = {
  display: 'flex',
  justifyContent: 'space-between'
}

export { styleLayoutService, styleTitle, styleServiceSectionLayout }
